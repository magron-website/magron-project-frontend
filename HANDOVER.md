# MAGRON 홈페이지 운영 매뉴얼 (인수인계 문서)

> 코딩을 몰라도 magron.kr을 이해하고 운영할 수 있도록 정리한 문서입니다.
> 보기 편한 버전(그림/색상 포함)은 별도로 전달된 웹 링크를 참고하세요. 이 파일은 그 텍스트 버전이며, 코드와 함께 git에 남아 다음 담당자가 저장소만 열어도 바로 찾을 수 있게 하기 위한 것입니다.
>
> 작성 기준: 2026-08-10 · 대상 저장소: `github.com/magron-website/magron-project-frontend` · 실서비스: `magron.kr`

---

## 0. 뭘 하고 싶으신가요? — 바로가기

| 하고 싶은 일 | 이동 |
|---|---|
| 메인 배너(맨 위 큰 슬라이드) 문구·사진 바꾸기 | [2장, hero_slides](#2-코딩-없이-지금-바로-고칠-수-있는-것--supabase) |
| 제품 소개 6개 타일 사진·제목·설명 바꾸기 | [2장, product_explanations](#2-코딩-없이-지금-바로-고칠-수-있는-것--supabase) |
| 카탈로그 PDF 파일 교체하기 | [2장, books](#2-코딩-없이-지금-바로-고칠-수-있는-것--supabase) |
| 기술정보 페이지에 자료 PDF 추가하기 | [2장, tech_documents](#2-코딩-없이-지금-바로-고칠-수-있는-것--supabase) |
| 제품 상세 페이지 안의 긴 설명글 수정 | [3장 (개발자 필요)](#3-개발자가-필요한-일) |
| 새 제품 페이지를 아예 새로 만들기 | [3장 (개발자 필요)](#3-개발자가-필요한-일) |
| 사이트가 안 열리거나 이상하게 보일 때 | [11장, 문제 해결](#11-문제-생겼을-때-체크리스트) |
| 이 사이트 로그인 정보 · 계정 목록 | [10장, 계정 총정리](#10-이-사이트를-움직이는-계정-총정리) |

---

## 1. 큰 그림 — 이 사이트는 어떻게 움직이는가

magron.kr은 겉보기엔 하나의 사이트지만, 실제로는 서로 독립된 3개의 시스템이 합쳐져서 동작합니다.

```
[방문자 브라우저]
      │  magron.kr 접속
      ▼
[① 정적 파일 — magron.kr, GitHub Pages]
   화면의 틀·디자인·버튼·일부 고정 텍스트가 담긴 코드 뭉치
      │  화면이 뜨자마자, 필요할 때마다 아래 두 곳에 인터넷으로 접속
      ├──────────────────────────────┐
      ▼                              ▼
[② 데이터베이스 — Supabase]     [③ 챗봇 서버 — Railway]
 배너 문구, 제품 사진, 카탈로그,   오른쪽 아래 챗봇 버튼을
 기술자료 등 대부분의 "내용"       눌렀을 때만 연결
```

- **① 정적 파일 (GitHub Pages)** — 사이트의 뼈대와 디자인. 개발자가 코드를 고치고 **배포**해야만 바뀝니다.
- **② Supabase** — 사이트에 보이는 글자·사진의 대부분이 저장된 곳. 여기는 **코딩 없이** 직접 바로 고칠 수 있습니다.
- **③ Railway (챗봇 서버)** — 챗봇이 답변을 만드는 별도 프로그램. 이 프로젝트(프론트엔드) 코드와는 완전히 분리된 다른 저장소입니다.

> **⚠️ 모든 텍스트가 Supabase에 있는 건 아닙니다.** 제품 상세 페이지의 긴 설명(예: 자성유체 키트의 "구성품별 상세" 문단들), 회사소개 문구, 하단 푸터의 주소·전화번호 같은 것들은 Supabase가 아니라 **코드 안에 직접** 쓰여 있습니다. 이런 건 개발자가 고쳐야 합니다 — 3장 참고.

---

## 2. 코딩 없이 지금 바로 고칠 수 있는 것 — Supabase

**Supabase**는 "온라인 엑셀표"라고 생각하시면 됩니다. 표의 한 칸(값)을 고치고 저장하면, **몇 초 안에 실제 magron.kr 화면에 그대로 반영**됩니다. 빌드니 배포니 하는 절차가 전혀 필요 없습니다.

접속: [supabase.com/dashboard](https://supabase.com/dashboard) → 로그인 → 프로젝트 선택 → 왼쪽 메뉴 `Table Editor`.

**기본 원칙**: 표 안의 기존 칸 내용만 수정하는 것은 안전합니다. 행(줄)을 새로 추가/삭제하는 것, `sort_order`(순서) 값을 바꾸는 것은 아래 "중요 규칙"을 꼭 읽고 하세요.

### hero_slides — 홈 화면 맨 위 큰 슬라이드 배너 (4개)

| 칸 | 의미 |
|---|---|
| `title` / `title_en` / `title_zh` | 큰 제목 (한/영/중) |
| `subtitle`, `description` (+`_en`/`_zh`) | 부제, 설명 문단 |
| `button_text` (+`_en`/`_zh`) | 버튼 글자 |
| `button_link` | 버튼 클릭 시 이동 주소 (예: `/company`). **이 표는 이동 주소도 직접 바꿀 수 있습니다.** |
| `image_url` | 배경 사진 주소 |
| `sort_order` | 배너 순서 |
| `is_active` | true=노출, false=숨김 |

### product_explanations — 홈 "제품 정보" 섹션 타일 6개

| 칸 | 의미 |
|---|---|
| `title`/`subtitle`/`description` (+`_en`/`_zh`) | 카드 제목·부제·설명 |
| `category` (+`_en`/`_zh`) | 제목 위 분류 글자 |
| `image_url` | 카드 배경 사진 |
| `sort_order` | **1~6 사이 숫자.** 아래 중요 규칙 필독 |
| `is_active` | 노출 여부 |

> **🛑 중요 규칙 — 이 표에는 "이동 주소" 칸이 없습니다.** `hero_slides`와 달리, 이 표는 카드 클릭 시 이동할 페이지를 정하는 칸이 없습니다. 대신 **`sort_order` 숫자(몇 번째 자리인가)로 이동할 페이지가 코드 안에 미리 정해져** 있습니다 (1=Ferrofluid, 2=Feedthrough, 3=Mag-Oil, 4=Magnet, 5=PIEZO Ink, 6=Display). 그래서 이 6개 행의 제목·사진·설명은 자유롭게 바꿔도 되지만, 행을 추가하거나 순서를 바꾸면 클릭 시 엉뚱한 페이지로 연결됩니다.
>
> **실제 있었던 일 — PIEZO Ink 사례**: 5번 자리 카드 제목만 "PIEZO Ink"로 바꿔놨는데, 클릭하면 계속 옛날 "자성유체 키트" 페이지가 나온 적이 있습니다. 원인은 5번 자리가 클릭됐을 때 이동할 페이지가 코드에 `/education`으로 고정되어 있었기 때문. 개발자가 `/piezo-ink` 새 페이지를 만들고 5번 자리 이동 주소를 그쪽으로 다시 연결해서 해결했습니다. 표 안의 글자만 바꿔서는 해결되지 않는 경우가 바로 이런 경우입니다.

### books — 홈 "카탈로그" 섹션 + 각 제품 페이지 카탈로그 다운로드 버튼

| 칸 | 의미 |
|---|---|
| `title`/`subtitle`/`description` | 카탈로그 카드 텍스트 |
| `image_url` | 표지 이미지 |
| `pdf_url` | **실제 다운로드되는 PDF 주소.** 새 카탈로그로 교체할 때 여기를 바꿈 |
| `sort_order` | 각 제품 페이지 다운로드 버튼과 짝짓는 기준 (product_explanations와 동일 규칙) |
| `is_active` | 노출 여부 |

### tech_documents — 기술정보 페이지 PDF 자료 목록

`title`/`description`(+`_en`/`_zh`), `file_url`(PDF 주소), `sort_order`, `is_active`.
새 기술 자료를 추가하려면 이 표에 **새 행을 추가**하면 됩니다 — product_explanations와 달리 행을 늘려도 안전합니다 (페이지가 이 표 내용을 그대로 나열하는 방식이라서).

### ferrofluid_info — 자성유체 관련 이미지 카드

`title`, `subtitle`, `description`, `image_url`, `category`, `sort_order`, `is_active` — 위 표들과 동일한 구조.

### display_videos — 대형 자성유체 디스플레이 페이지 영상

`video_url`(영상 파일 주소), `title`, `subtitle`, `description`, `sort_order`, `is_active`.

### 이미지 · PDF · 영상 파일 자체를 교체하는 법

위 표들의 `image_url`/`pdf_url`/`video_url`에는 파일이 직접 들어있는 게 아니라 **Supabase Storage(파일 창고)에 올려둔 파일의 주소**가 적혀 있습니다.

1. **새로 올리고 주소를 바꾸는 방법 (권장)**: Supabase 왼쪽 메뉴 `Storage` → `magron-website` → 해당 폴더에 새 파일 업로드 → 업로드된 파일의 "주소 복사" → 표의 해당 칸에 붙여넣기.
2. **같은 이름으로 덮어쓰는 방법**: 기존 파일을 삭제하고 정확히 같은 파일명으로 새 파일을 올리면 표의 주소를 안 바꿔도 자동 반영됩니다.

저장 후 화면이 안 바뀐다면 브라우저 캐시 문제일 수 있습니다 — `Ctrl+Shift+R`(강력 새로고침)으로 확인하세요.

---

## 3. 개발자가 필요한 일

Supabase 표를 고쳐서 되는 일이 아니라 **코드 자체를 고치고 다시 배포**해야 하는 일:

- 제품 상세 페이지 안의 긴 설명글 (자성유체 키트 구성품별 상세, Magnet 소재별 특성표 등)
- 새로운 제품/서비스를 위한 **새 페이지** (이번에 만든 PIEZO Ink 페이지처럼)
- 메뉴 구조, 디자인, 색깔, 레이아웃 변경
- 회사소개(About) 문구, 푸터의 주소·전화번호·이메일
- 챗봇이 답변하는 내용 자체 (별도 서버 — 8장)
- 새 언어 추가 (예: 일본어)

이런 요청은 "어디를, 어떻게 바꾸고 싶은지"를 구체적으로 적어 전달하면 작업이 빨라집니다.

### 지금 알려진 미완성 항목 (다음 담당자가 이어서 처리할 것)

- [ ] PIEZO Ink 페이지(`/piezo-ink`) — 지금은 임시 문구·임시 사진 상태. 실제 자료 도착하면 `src/i18n/locales/piezoInk.ts` 텍스트와 `src/pages/products/PiezoInk.tsx`의 이미지 교체 필요
- [ ] 기존 "자성유체 키트" 페이지(`/education`) — 홈 타일에서는 빠졌지만 페이지와 헤더 메뉴에는 남아있음. 유지할지 정리할지 결정 필요
- [ ] 푸터의 LinkedIn 아이콘 — 실제 링크 미연결 (`src/components/Footer.tsx`)

---

## 4. 코드 수정 → 실제 사이트 반영까지 (배포 흐름)

Supabase는 저장하면 바로 반영되지만 코드 수정은 다릅니다. 이 저장소는 `docs/` 폴더를 GitHub Pages가 그대로 웹사이트로 서빙하는 방식이라, 별도 서버 배포 절차 없이 **GitHub에 올리는 순간이 곧 배포**입니다.

```
1. npm run build         # 코드를 실제 웹사이트 파일로 다시 만듦 (docs/ 폴더 생성)
2. git add .              # 바뀐 파일들을 표시
3. git commit -m "설명"   # 변경 내용을 하나의 묶음으로 저장
4. git push origin main   # GitHub에 업로드 → 1분 안에 magron.kr에 반영
```

> `git push`는 실제 사이트에 바로 반영되므로, push 전에 로컬(5장)에서 화면을 먼저 확인하는 것이 안전합니다.

`npm run build`는 내부적으로 `tsc -b`(타입 검사) → `vite build`(실제 빌드) → `scripts/prerender.mjs`(검색엔진용 페이지 생성) 세 단계를 순서대로 실행합니다.

---

## 5. 내 컴퓨터에서 직접 열어보기 (로컬 개발 환경)

- **Node.js 설치** — 코드를 실행하는 프로그램, 한 번만 설치 (nodejs.org)
- **`install.bat`** 더블클릭 — 필요한 부품을 처음 한 번 내려받음
- **`dev.bat`** 더블클릭 — 미리보기 서버 실행. 창에 뜨는 주소(보통 `http://localhost:5173`)를 브라우저에 입력. **여기서 뭘 눌러도 실제 magron.kr에는 영향 없음.**

> **`.env` 파일이 꼭 있어야 합니다.** Supabase 접속 "열쇠"가 담긴 `.env`는 보안상 GitHub에 올라가지 않습니다. 새 컴퓨터로 프로젝트를 옮기면 이 파일을 별도로 복사해서 프로젝트 최상위 폴더에 넣어야 미리보기가 정상 동작합니다 — 10장 참고.

---

## 6. 프로젝트 폴더 전체 지도

| 위치 | 여기에 뭐가 있나 |
|---|---|
| `src/pages/` | 화면 단위 (홈, 회사소개, 기술정보) + `products/` 하위 제품별 상세 페이지 |
| `src/components/` | 여러 화면에서 재사용되는 조각 (헤더, 푸터, 챗봇 창 등) |
| `src/i18n/locales/` | **코드에 고정된 모든 글자** (한/영/중). 파일 하나 = 화면 하나의 텍스트 뭉치 |
| `src/assets/design/` | 디자인(색깔, 여백, 레이아웃) CSS |
| `src/assets/images/` | 코드에 직접 포함된 이미지 (Supabase가 아닌 내장 사진) |
| `src/hooks/` | Supabase에서 데이터를 가져오는 연결 로직 |
| `src/lib/supabase.ts` | Supabase 접속 설정 (`.env`에서 주소·열쇠를 읽음) |
| `scripts/` | 배포용 자동화 스크립트 (검색엔진용 페이지 생성 등) |
| `docs/` | **실제로 배포되는 결과물.** 사람이 직접 손대지 않고 `npm run build`가 자동 생성 |
| `.env` | Supabase 접속 정보 (비공개, git에 포함 안 됨) |
| `install.bat` / `dev.bat` | 비개발자도 더블클릭으로 로컬 미리보기를 켤 수 있는 실행 파일 |

---

## 7. 3개 국어(한/영/중) 시스템

오른쪽 위 국기 버튼으로 언어를 바꾸면 코드에 고정된 텍스트가 통째로 바뀝니다. 각 언어별 문구는 `src/i18n/locales/` 폴더 안 파일마다 `ko`/`en`/`zh` 세 묶음으로 나란히 적혀 있습니다.

Supabase 표들도 `title`/`title_en`/`title_zh`처럼 언어별 칸이 따로 있어서, 세 언어 모두 채워야 그 언어에서도 내용이 보입니다. 하나라도 비워두면 그 언어에서는 빈칸으로 나옵니다.

---

## 8. 챗봇은 어떻게 동작하나

화면 오른쪽 아래 챗봇 버튼은 이 프론트엔드 코드와 **완전히 다른 별도 프로그램**(Railway에서 실행 중)에 질문을 보내고 답을 받아옵니다.

- 주소: `magron-website-backend-production-1289.up.railway.app`
- 연결 코드 위치: `src/hooks/useChat.ts`
- 챗봇이 무슨 내용을 알고 어떻게 답하는지는 **이 저장소 밖의 다른 백엔드 프로젝트**에서 관리됩니다. 이름은 `magron-website-backend`로 보이는데, 이 컴퓨터에는 실제 폴더가 남아있지 않아 **어디에 있는지 별도 확인이 필요**합니다 (10·12장 참고).

---

## 9. 도메인 · 검색엔진(SEO)

- **도메인**: magron.kr → GitHub Pages로 연결 (`docs/CNAME` 파일에 도메인이 적혀 있는 방식). 도메인 등록·갱신 업체(가비아, 후이즈 등)는 이 코드와 별개이므로 **어느 업체인지 확인해서 10장에 채워 넣어야 함**.
- **SEO**: 새 페이지를 추가하면 `src/seo/routeMeta.ts`와 `scripts/prerender.mjs` 두 곳에 그 페이지의 제목·설명을 똑같이 추가해야 검색 결과에도 정상 노출됩니다 (하나만 고치면 어긋남).

---

## 10. 이 사이트를 움직이는 계정 총정리

> **비밀번호는 이 문서에 적지 않았습니다.** 아래 표는 "무엇이 어디 있는지"만 정리한 것입니다. 실제 아이디·비밀번호는 회사 비밀번호 관리 방식으로 별도 전달하세요.

| 서비스 | 용도 | 확인된 정보 |
|---|---|---|
| GitHub | 코드 저장 + 자동 배포(Pages) | github.com/magron-website/magron-project-frontend |
| Supabase | 콘텐츠 데이터베이스 (2장의 표 6개) | 프로젝트: oxziijsqpiasesuqtxhs.supabase.co |
| Railway | 챗봇 답변 서버 호스팅 | magron-website-backend-production-1289.up.railway.app — 소스 코드 저장소 위치 확인 필요 |
| 도메인 등록기관 | magron.kr 소유·갱신 | 어느 업체인지 확인 필요 |
| 업무 이메일 | 사이트 문의 수신 | magron@magron.co.kr |

---

## 11. 문제 생겼을 때 체크리스트

| 증상 | 가장 먼저 확인할 것 |
|---|---|
| 제품 정보·배너가 텅 비어있음 | Supabase 접속 문제 가능성. 로그인 정상 여부, 해당 표 `is_active` 확인 |
| 사진이 깨져서 안 보임 | Storage에서 이미지 파일 삭제/이동 가능성. `image_url`을 새 탭에 직접 열어 확인 |
| 카탈로그 다운로드가 안 됨 | `books` 표의 `pdf_url` 값 확인 |
| Supabase에서 고쳤는데 사이트에 그대로임 | 브라우저 캐시. `Ctrl+Shift+R` 강력 새로고침 |
| 코드를 고쳤다는데 사이트에 안 보임 | 4장 배포 순서(build→commit→push)를 끝까지 했는지 확인 |
| 챗봇이 응답 안 함 | Railway 서버 자체 문제 가능성. 별도 백엔드 프로젝트 쪽 확인 필요 (8장) |

---

## 12. 인수인계 체크리스트

- [ ] GitHub 저장소에 새 담당자를 **공동 작업자(Collaborator)**로 추가
- [ ] Supabase 프로젝트에 새 담당자를 멤버로 추가 (또는 로그인 정보 안전하게 전달)
- [ ] Railway(챗봇 서버) 접근 권한 및 **그 백엔드 프로젝트의 실제 소스코드 위치** 확인·전달
- [ ] 도메인(magron.kr) 등록기관 로그인 정보 확인·전달
- [ ] `.env` 파일 내용(Supabase 접속 열쇠) 새 담당자 컴퓨터에도 별도 전달 — git에는 없는 파일이라 꼭 따로 챙겨야 함
- [ ] 이 문서와 프로젝트 폴더 전체를 새 담당자 컴퓨터(또는 회사 공용 저장소)에 백업
- [ ] 3장 "지금 알려진 미완성 항목" 진행 상황 공유
