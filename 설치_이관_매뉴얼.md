# 마그론 홈페이지 — 다른 컴퓨터로 이관 & 설치 매뉴얼

> 이 폴더(magron 홈페이지 프로젝트)를 USB로 이사님 컴퓨터에 옮겨서
> **미리보기 → 수정 → 배포**까지 할 수 있게 만드는 방법입니다.
> 작성: 2026-08-11

---

## 0. 한눈에 보기

- 이 사이트 = **코드(GitHub Pages) + Supabase(콘텐츠) + Railway(챗봇)** 3개로 굴러갑니다.
- **글자·사진·PDF 대부분**은 Supabase에서 코딩 없이 바로 수정 → 몇 초 만에 반영.
- **제목/섹션 배치/디자인 등 코드에 박힌 것**은 이 폴더에서 고치고 `배포(build+push)` 해야 반영.
- 이 매뉴얼은 그 "코드 쪽"을 이사님 컴퓨터에서도 할 수 있게 옮기는 방법입니다.

---

## 1. USB에 담을 것 (이 폴더 통째로)

이 폴더(`magron-current`)를 그대로 USB에 복사하면 됩니다. 단, 아래만 주의:

- ✅ **꼭 포함**: `src/`, `docs/`, `public/`, `scripts/`, `package.json`, `.git/`(배포용), **`.env`(중요!)**
- ⛔ **빼도 됨**: `node_modules/` 폴더 (용량 큼, 새 컴퓨터에서 `install.bat`으로 다시 받음)

> **`.env` 파일이 제일 중요합니다.** Supabase 접속 열쇠가 들어있고 GitHub에는 안 올라가는 파일이라,
> USB로 직접 옮기지 않으면 새 컴퓨터에서 사이트 데이터가 안 뜹니다.
> (내용: Supabase 주소 + 공개키 2줄. 공개키라 보안 위험은 낮음)

**이미 정리된 압축본**: `D:\magron-홈페이지-이관패키지.zip` (node_modules 제외, .env 포함) — 이걸 USB에 복사하면 끝.

---

## 2. 이사님 컴퓨터에서 처음 한 번 세팅

### (1) Node.js 설치
- https://nodejs.org 에서 **LTS 버전** 다운로드 → 설치 (한 번만).
- 확인: 명령 프롬프트에서 `node -v` 입력 시 버전이 나오면 성공.

### (2) 폴더 복사
- USB의 프로젝트 폴더(또는 zip 압축 해제)를 원하는 위치에 복사.
  예: `C:\Users\(사용자)\projects\magron-current`

### (3) 부품 설치
- 폴더 안 **`install.bat` 더블클릭** → 필요한 부품(node_modules) 자동 설치 (인터넷 필요, 몇 분).
- (수동으로 하려면 폴더에서 명령 프롬프트 열고 `npm install`)

### (4) 미리보기 실행
- **`dev.bat` 더블클릭** → 창에 `http://localhost:5173` 주소가 뜸 → 브라우저에 입력.
- **여기서 뭘 하든 실제 magron.kr에는 영향 없음** (내 컴퓨터 안 미리보기).

> 미리보기에서 콘텐츠가 안 뜨면 → `.env`가 폴더 최상위에 있는지 확인.

---

## 3. 내용 수정하는 법

### A. Supabase에서 (코딩 없이, 즉시 반영)
- supabase.com/dashboard → Continue with GitHub(magron21) → 프로젝트 → Table Editor / Storage.
- 이미지·PDF 교체, 순서, 노출 on/off 등 → 저장하면 몇 초 내 magron.kr 반영.
- 자세한 표 구조는 같은 폴더 `HANDOVER.md` 2장 참고.

### B. 코드에서 (제목·설명·배치 등 → 배포 필요)
- 제목/설명 글자는 대부분 `src/i18n/locales/` 안에 있음.
  - 예) 카탈로그 제목 = `home.ts`의 `catalog.items`
  - 예) 기술자료 제목·설명 = `tech.ts`의 `documents`
- 고친 뒤 아래 4번 "배포"를 해야 실제 사이트에 반영됨.
- `dev.bat` 미리보기로 먼저 확인하고 배포하는 게 안전.

---

## 4. 배포하는 법 (코드 수정 → 실제 magron.kr 반영)

명령 프롬프트를 **프로젝트 폴더에서 열고** 순서대로:

```bash
npm run build
git add -A
git commit -m "무엇을 바꿨는지 설명"
git push origin main
```

- `git push` 하는 순간 GitHub Pages가 약 1분 안에 magron.kr에 반영합니다.
- **`git push`에는 `magron-website` 저장소 write 권한이 있는 GitHub 계정 로그인이 필요**합니다.
  처음 push 시 브라우저 로그인 창이 뜨면 권한 있는 계정(magron21 등)으로 로그인.

### ⚠️ 배포 전 딱 한 번 필요한 것 — Playwright
`npm run build`의 마지막 단계(검색엔진용 사전렌더)는 Playwright 브라우저가 필요합니다.
새 컴퓨터에서 처음 빌드하기 전에 한 번만:

```bash
npx playwright install chromium
```

- 안 깔면 빌드는 되지만 SEO용 본문 사전렌더가 "meta-only"로 약해집니다(사이트 동작 자체엔 문제 없음).

---

## 5. 필요한 계정 / 권한 체크리스트

| 서비스 | 왜 필요 | 확인 |
|---|---|---|
| **GitHub** (magron-website 조직) | 코드 배포(push) | magron21이 write 권한 보유 ✅ / 조직 admin이 누구인지 확인 권장 |
| **Supabase** (magron21's Org) | 콘텐츠 수정 | Continue with GitHub로 로그인 ✅ |
| **Railway** (챗봇 서버) | 챗봇 답변 수정 | ⚠️ magron21 Railway는 비어있음. 실제 서버는 다른 계정 — 위치 확인 필요 |
| **도메인 등록기관** | magron.kr 갱신 | 어느 업체인지 확인 필요 |

- Supabase 키·구조 상세: 같은 폴더 밖 `마그론 홈페이지\마그론홈페이지_구성과_키_안내.md` 참고.
- 전체 운영 매뉴얼: 같은 폴더 `HANDOVER.md` 참고.

---

## 6. 문제 생기면

| 증상 | 확인 |
|---|---|
| 미리보기(localhost)에 콘텐츠 안 뜸 | `.env` 파일이 폴더 최상위에 있는지 |
| `install.bat`/`dev.bat`이 안 켜짐 | Node.js 설치됐는지 (`node -v`) |
| `git push`가 거부됨 | 로그인 계정에 magron-website 저장소 write 권한 있는지 |
| 고쳤는데 사이트에 안 보임 | 4번 배포(build→commit→push)를 끝까지 했는지 / 브라우저 `Ctrl+Shift+R` |
| 챗봇 응답 안 함 | Railway(별도 서버) 문제 — 이 폴더와 무관 |
