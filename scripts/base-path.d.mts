// base-path.mjs stays plain JS so scripts/prerender.mjs can run it under bare
// node; this declaration is only so vite.config.ts can import it type-checked.
export declare const BASE_PATH: string
export declare const BASE_PREFIX: string
